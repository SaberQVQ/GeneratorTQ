package ${BasePackageName}${businessType}.${EntityName}.${DaoPackageName};

import org.springframework.stereotype.Repository;
import com.tp.base.mapper.BaseMapper;

/**
 * Author ${Author}
 * Date  ${Date}
 */
@Repository
public interface ${ClassName}Mapper extends BaseMapper{

}